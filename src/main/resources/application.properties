spring.application.name=graphiql
#Enabling graphql playground
spring.graphql.graphiql.enabled=true
spring.graphql.graphiql.path=/graphiql


# Use H2 in-memory database
spring.datasource.url=jdbc:h2:mem:myTestDB
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.sql.init.mode=always
spring.datasource.initialization-mode=always

# Let Spring create the schema automatically
spring.jpa.hibernate.ddl-auto=create

# Show SQL in console
spring.jpa.show-sql=true

# ===============================
# H2 CONSOLE CONFIGURATION
# ===============================

# Enable the H2 web console
spring.h2.console.enabled=true

# H2 Console path
spring.h2.console.path=/h2-console
#################################################################
# grapgql : http://localhost:8080/graphiql?path=/graphql
# http://localhost:8080/h2-console/
#postman : http://localhost:8080/graphql -> new graphql request

